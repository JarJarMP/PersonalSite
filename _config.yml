# change source and destination folders for building site
source: ./jekyll_source
destination: ./jekyll_result

# set up default layout
defaults:
  -
    scope:
      path: ""
    values:
      layout: "default"